<!doctype html>
<html lang="fr">
<%- include("header"); -%>
<body class="bodyColor">
<%- include("barre"); -%>

<main>
    <section>
        <div class="container-fluid">
            <div class="container">
                <div class="row-align-self-center">
                    <article class=" col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <div class="table-responsive">
                            <table class="table table-striped table-dark">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Prenom</th>
                                    <th scope="col">Poste</th>
                                    <th scope="col">Apparitions</th>
                                    <th scope="col">Buts encaissés</th>
                                    <th scope="col">Note moyenne</th>
                                </tr>
                                </thead>
                                <tbody>
                                <% for( i in player.api.players){ %>
                                    <%if(player.api.players[i].league === "Ligue 1"){%>
                                        <%if(player.api.players[i].position === "Goalkeeper"){%>
                                    <tr>
                                        <th scope="row"><%= player.api.players[i].lastname%></th>
                                        <td><%= player.api.players[i].firstname%></td>
                                        <td> Gardien </td>
                                        <td><%= player.api.players[i].games.appearences%></td>
                                        <td><%= player.api.players[i].goals.conceded%></td>
                                        <td><%= player.api.players[i].rating%></td>
                                    </tr>
                                <% }}} %>

                                </tbody>
                            </table>
                        </div>
                    </article>
                    <br>
                    <article class=" col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <div class="table-responsive">
                            <table class="table table-striped table-dark">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Prenom</th>
                                    <th scope="col">Poste</th>
                                    <th scope="col">Apparitions</th>
                                    <th scope="col">Buts</th>
                                    <th scope="col">Passes</th>
                                    <th scope="col">Note moyenne</th>
                                </tr>
                                </thead>
                                <tbody>
                                <% for( i in player.api.players){ %>
                                <%if(player.api.players[i].league === "Ligue 1"){%>
                                <%if(player.api.players[i].position === "Defender"){%>
                                    <tr>
                                        <th scope="row"><%= player.api.players[i].lastname%></th>
                                        <td><%= player.api.players[i].firstname%></td>
                                        <td>Defenseur</td>
                                        <td><%= player.api.players[i].games.appearences%></td>
                                        <td><%= player.api.players[i].goals.total%></td>
                                        <td><%= player.api.players[i].goals.assists%></td>
                                        <td><%= player.api.players[i].rating%></td>
                                    </tr>
                                <% }}} %>

                                </tbody>
                            </table>
                        </div>
                    </article>
                    <br>

                    <article class=" col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <div class="table-responsive">
                            <table class="table table-striped table-dark">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Prenom</th>
                                    <th scope="col">Poste</th>
                                    <th scope="col">Apparitions</th>
                                    <th scope="col">Buts</th>
                                    <th scope="col">Passes</th>
                                    <th scope="col">Note moyenne</th>
                                </tr>
                                </thead>
                                <tbody>
                                <% for( i in player.api.players){ %>
                                <%if(player.api.players[i].league === "Ligue 1"){%>
                                <%if(player.api.players[i].position === "Midfielder"){%>
                                    <tr>
                                        <th scope="row"><%= player.api.players[i].lastname%></th>
                                        <td><%= player.api.players[i].firstname%></td>
                                        <td>Milieu de terrain</td>
                                        <td><%= player.api.players[i].games.appearences%></td>
                                        <td><%= player.api.players[i].goals.total%></td>
                                        <td><%= player.api.players[i].goals.assists%></td>
                                        <td><%= player.api.players[i].rating%></td>
                                    </tr>
                                <% }}} %>

                                </tbody>
                            </table>
                        </div>
                    </article>
                    <br>
                    <article class=" col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <div class="table-responsive">
                            <table class="table table-striped table-dark">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Prenom</th>
                                    <th scope="col">Poste</th>
                                    <th scope="col">Apparitions</th>
                                    <th scope="col">Buts</th>
                                    <th scope="col">Passes</th>
                                    <th scope="col">Note moyenne</th>
                                </tr>
                                </thead>
                                <tbody>
                                <% for( i in player.api.players){ %>
                                <%if(player.api.players[i].league === "Ligue 1"){%>
                                <%if(player.api.players[i].position === "Attacker"){%>
                                    <tr>
                                        <th scope="row"><%= player.api.players[i].lastname%></th>
                                        <td><%= player.api.players[i].firstname%></td>
                                        <td>Attaquant</td>
                                        <td><%= player.api.players[i].games.appearences%></td>
                                        <td><%= player.api.players[i].goals.total%></td>
                                        <td><%= player.api.players[i].goals.assists%></td>
                                        <td><%= player.api.players[i].rating%></td>
                                    </tr>
                                <% }}} %>

                                </tbody>
                            </table>
                        </div>
                    </article>
                </div>
            </div>
        </div>
    </section>
</main>
</body>
<%- include("footer");-%>
</html>